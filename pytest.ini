[pytest]
testpaths = backend/tests
pythonpath = backend
markers =
    unit: Unit tests (fast, isolated, offline)
    integration: Integration tests (may require external dependencies such as TiDB Cloud)
